<!-- %= render 'form', automation: @automation.becomes(CardOrder), url: automation_path(@automation) % -->
<%= render 'form', automation: @automation, aws_sign_endpoint: @aws_sign_endpoint, return_address: @return_address, url: automation_path(@automation) %>
