<%= tag.div id: "automation-editor",
            data: {
                id: automation.id,
                automation: automation.to_json(only: [:id, :type, :discount_pct, :discount_exp, :enabled, :international, :send_delay, :front_json, :back_json, :filters, :budget_type, :budget]),
                filters: automation.filters.to_json(only: [:id, :filter_data]),
                aws_sign_endpoint: aws_sign_endpoint,
                back_url: _back_url
            }
%>
