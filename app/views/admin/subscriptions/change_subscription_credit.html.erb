<%= form_for @subscription, url: change_credit_admin_subscription_path(@subscription), method: :put do |f| %>
  Credit: <%= number_field_tag :value, @subscription.value, step: '0.01' %><br />
  <br />
  <%= f.submit %><%= link_to "Cancel", :back %>
<% end %>
