(function() {
  var id = "#postcard-" + <%= @postcard.id %>;
  var row = document.querySelector(id);
  var infoBtn = document.querySelector(`${id} span`);
  var cancelBtn = document.querySelector(`${id} a`);
  row.querySelector('td').removeChild(cancelBtn);
  infoBtn.innerHTML = "Canceled";
})();
